[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: Mark a test as a unit test
    integration: Mark a test as an integration test
    slow: Mark a test as slow (will be skipped by default)